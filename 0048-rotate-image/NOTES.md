for **CLOCK** wise first reverese matrix up to down.
1. **reverse**(matrix.begin(), matrix.end());
2. **transpose i=0; j = i+1**
​
​
for **ANTI-CLOCK** wise:
1. **transpose i=0; j = i+1**
2. **reverse**(matrix.begin(), matrix.end());
​